class NpcGroup extends Group<Npc> {
  
  NpcGroup(World world) { 
    super(world);
  }

  void addNpc(float x, float y) {
    //Npc npc = new Npc(x, y);
    //world.register(npc, true);
    //this.add(npc);
  }

  void generateNpc() {
    // NETWORK stuff
    //addNpc(200, 200);
  }
}
